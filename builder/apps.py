"""Django app configuration for the builder app."""

from django.apps import AppConfig


class BuilderConfig(AppConfig):
    """Registers app metadata used by Django startup."""

    name = 'builder'
